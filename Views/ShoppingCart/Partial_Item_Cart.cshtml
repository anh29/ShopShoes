@model IEnumerable<ShopLearning.Models.EF.ShoppingCartItem>
@using ShopLearning.Models.Common
@*<table class="table">
    <tr class="text-center">
        <th>STT</th>
        <th>Ảnh sản phẩm</th>
        <th>Tên sản phẩm</th>
        <th>Danh mục sản phẩm</th>
        <th>Giá</th>

        <th style="width:100px;">Số lượng</th>
        <th>Thành tiền</th>
        <th style="width:400px;"></th>
    </tr>
*@
    @if(Model!=null && Model.Any())
            {
                         var i=0;
                        var total = decimal.Zero;
                foreach(var item in Model)
        {

                total += item.TotalPrice;
            <tr class="text-center" id="trow_@item.ProductId">
                <td>@(i+1)</td>
                <td><img src="@item.ProductImg" width="50" /></td>
                <td><a href="/chi-tiet/@item.Alias-p@(item.ProductId)">
                @item.ProductName
                </a>
                </td>
                <td>@item.CategoryName</td>
                <td>@Common.FormatNumber(item.Price, 0)</td>
                <td><input type="number" class="form-control" value="@item.Quantity"/></td>
                <td>@Common.FormatNumber(item.Price, 2)</td>
                <td>
                        <a href="#" data-id="@item.ProductId" class="btn btn-sm btn-danger">Xóa</a>
                        <a href='#' data-id="@item.ProductId" class="btn btn-sm btn-success">Cập nhật</a>
                </td>
            </tr>
                        i++;
        }
                        <tr>
                            <th colspan="6" class="text-right">Tổng : </th>
                            <th class="text-center"> @Common.FormatNumber(total, 0)</th>
                            <th></th>
                        </tr>
                        
            }
                    else{
                        <tr>
                            <th colspan="7">Không có sản phẩm nào trong giỏ hàng </th>
                        </tr>
                        }
    
@*    <tr class="text-center">
        <td>1</td>
        <td><img src="~/Content/assets/images/mwc (5).jpg" width="50" /></td>
        <td>Giày thể thao Sneaker 25C12</td>
        <td>Giày MLB</td>
        <td>600.000VNĐ</td>
        <td><input type="number" class="form-control" value="1" /></td>
        <th>@Common.FormatNumber(200000, 2)</th>
        <td>
            <a href="#" class="btn btn-sm btn-danger btnDelete">Xóa</a>
            <a href="#" class="btn btn-sm btn-success btnUpdate">Cập nhật</a> 
        </td>
    </tr>
    <tr>
        <th colspan="6" class="text-right">Tổng : </th>
        <th class="text-center">@Common.FormatNumber(10000000, 0)</th>
        <th></th>
    </tr>
   
</table>*@