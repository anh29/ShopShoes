@*xác định rằng kiểu mô hình (model) của trang web là một danh sách các đối tượng ProductCategory.*@
@model IEnumerable<ShopOnline.Models.EF.ProductCategory>

@if (Model != null && Model.Any())
{
    var i = 0;
    <!-- Banner -->

    <div class="banner">
        <div class="container">
            <div class="row">
                @foreach (var item in Model)
                {
                    i++;
                    if (i <= 3)
                    {
                    <div class="col-md-4">
                        <div class="banner_item align-items-center" style="background-image: url('@item.Icon')">
                            <div class="banner_category">
                                <a href="@item.Alias">@item.Title</a>
                            </div>
                        </div>
                    </div>
                    }
                }
                @*<div class="col-md-4">
                    <div class="banner_item align-items-center" style="background-image: url('/Content/assets/images/banner_1.jpg')">
                        <div class="banner_category">
                            <a href="categories.html">accessories's</a>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="banner_item align-items-center" style="background-image: url('/Content/assets/images/banner_1.jpg')">
                            <div class="banner_category">
                                <a href="categories.html">men's</a>
                            </div>
                    </div>
                        </div>
                    </div>*@
            </div>
        </div>
    </div>

}
