@model ShopOnline.Models.EF.News
@{
    ViewBag.Title = "Chi tiết tin tức";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link rel="stylesheet" type="text/css" href="~/Content/assets/styles/main_styles.css">
<style>
    .news_section_container {
        margin-top: 180px;
    }

    .row-item-new {
        margin-bottom: 20px;
    }

    .title-news {
        font-size: 24px;
        border-bottom: 1px solid;
        padding: 15px 0;
        margin-bottom: 25px;
    }
    .desc-news {
        font-size: 20px;
        padding: 15px 0;
    }
</style>
<div class="container news_section_container">
    @if (Model != null)
    {
        <div class="row">
            <div class="col-md-12">
                <h1 class="title-news">@Model.Title</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-md-2">
                <img src="@Model.Image" alt="@Model.Image" style="width: 90%;" />
            </div>
            <div class="col-md-10">
                <h2 class="desc-news">@Model.Description</h2>
                <div class="col-md-12">
                    @Html.Raw(Model.Detail)
                </div>
            </div>
        </div>
    }
</div>